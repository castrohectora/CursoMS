spring:
  application:
    name: user-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

  datasource:
    url: jdbc:mysql://localhost:3306/users?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&useLegacyDateTimeCode=false

    #username and password de la base de datos
    username: root
    password: #90ch0la9ant3ra

  #Mostrar SQL queries que se realizan
  jpa:
    show-sql: true

    #Actualiza la base de datos y crea la entidad
    hibernate:
      ddl-auto: update

    #generate optimization hibernate SQL
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect