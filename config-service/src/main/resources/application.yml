server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: keylcloak
          uri: https://github.com/castrohectora/CursoMS
          search-paths: config-data
  application:
    name: config-service

