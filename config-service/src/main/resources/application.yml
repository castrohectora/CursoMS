server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: ${CONFIG_GIT_LABEL:config-server-docker}
          uri: ${CONFIG_GIT_URI:https://github.com/castrohectora/CursoMS/tree/config-server-docker/config-data}

          refreshRate: 30
