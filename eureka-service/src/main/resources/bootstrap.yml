spring:
  application:
    name: eureka-service
  cloud:
    config:
      enabled: true
      uri: http://${CONFIG_HOST}:${CONFIG_PORT}
      import: optional:config-service:http://${CONFIG_HOST}:${CONFIG_PORT}