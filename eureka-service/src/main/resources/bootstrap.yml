server:
  port: 8761
spring:
  application:
    name: eurekaservice
  cloud:
    config:
      enabled: true
      uri: http://${CONFIG_HOST}:${CONFIG_PORT}
  config:
    import: configservice:http://${CONFIG_HOST}:${CONFIG_PORT}
