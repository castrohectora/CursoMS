server:
  port: 8090
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url: 
      default-zone: http://${DISCOVERY_HOST:localhost}:${DISCOVERY_PORT:8090}/eureka
